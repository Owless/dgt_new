# 🚀 Инструкция по установке и запуску DGT Exhaust System

## Быстрый старт

### Шаг 1: Установка Node.js

Убедитесь, что у вас установлен Node.js версии 18 или выше:
```bash
node --version
```

Если Node.js не установлен, скачайте его с [nodejs.org](https://nodejs.org/)

### Шаг 2: Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
npm install
```

Это установит все необходимые зависимости:
- Next.js 14
- React 18
- TypeScript 5

### Шаг 3: Добавление изображений

**ВАЖНО:** Перед запуском добавьте изображения в папку `public/`

Необходимые файлы (см. `public/IMAGES_README.md`):
- main_image.png
- descarga.png
- vyhlop.png
- disk.png
- disks.png
- podveska.png
- Bild_15-1024x576.jpg
- bmw.jpg
- 4QHL3OSVP1kmJToP.jpg
- Audi-RS6-C8-Riviera-RF108-Gloss-Black-29-1350x900.jpg
- images (4).jpeg
- 5ee9d286b50c320001032d99.jpeg

### Шаг 4: Запуск dev-сервера

```bash
npm run dev
```

Сайт будет доступен по адресу: http://localhost:3000

### Шаг 5: Сборка для продакшена

```bash
npm run build
npm start
```

## Структура проекта

```
dgt-exhaust/
├── app/                    # App Router (Next.js 14)
│   ├── layout.tsx         # Общий layout для всех страниц
│   ├── page.tsx           # Главная страница (/)
│   ├── page.module.css    # Стили главной страницы
│   ├── globals.css        # Глобальные стили
│   │
│   ├── exhaust/           # /exhaust - Выхлопные системы
│   │   └── page.tsx
│   ├── brakes/            # /brakes - Тормозные системы
│   │   └── page.tsx
│   ├── wheels/            # /wheels - Кованые диски
│   │   └── page.tsx
│   ├── suspension/        # /suspension - Подвеска
│   │   └── page.tsx
│   ├── projects/          # /projects - Проекты
│   │   └── page.tsx
│   ├── catalog/           # /catalog - Каталог услуг
│   │   └── page.tsx
│   ├── about/             # /about - О нас
│   │   └── page.tsx
│   ├── video/             # /video - Видеоматериалы
│   │   └── page.tsx
│   ├── contacts/          # /contacts - Контакты
│   │   └── page.tsx
│   ├── privacy/           # /privacy - Политика конфиденциальности
│   │   └── page.tsx
│   └── terms/             # /terms - Условия использования
│       └── page.tsx
│
├── components/            # Переиспользуемые компоненты
│   ├── UnderConstruction.tsx
│   └── UnderConstruction.module.css
│
├── public/                # Статические файлы
│   ├── IMAGES_README.md
│   └── [изображения]
│
├── package.json
├── tsconfig.json
├── next.config.js
└── README.md
```

## Функционал

### Главная страница (/)

✅ **Полностью функциональная**

Включает все секции:
1. **Hero** - Главный экран с призывом к действию
2. **О нас** - Информация о компании
3. **Выхлопные системы** - Секция с продуктами
4. **Тормозные системы** - Секция с продуктами
5. **Кованые диски** - Секция с продуктами
6. **Подвеска** - Секция с продуктами
7. **Проекты** - Галерея выполненных проектов
8. **Видео** - Видеоматериалы
9. **Footer** - Контакты и ссылки

**Особенности:**
- Переключение языка RU/EN
- Плавная прокрутка по якорям
- Интерактивные анимации
- Адаптивный дизайн

### Остальные страницы

🔧 **Заглушки "В разработке"**

Все страницы используют компонент `UnderConstruction` с:
- Анимированным фоном
- Описанием раздела
- Кнопкой возврата на главную

## Как заменить заглушки на реальный контент

### Пример: Страница "Выхлопные системы"

1. Откройте `app/exhaust/page.tsx`

2. Замените содержимое:

```tsx
// Было:
import UnderConstruction from '@/components/UnderConstruction'

export default function ExhaustPage() {
  return (
    <UnderConstruction
      title="Выхлопные системы"
      description="..."
    />
  )
}

// Стало:
export default function ExhaustPage() {
  return (
    <div>
      {/* Ваш контент здесь */}
      <h1>Выхлопные системы</h1>
      {/* ... */}
    </div>
  )
}
```

3. Создайте стили в `app/exhaust/page.module.css` (опционально)

4. Используйте стили из главной страницы как референс

## Технологии

- **Next.js 14** - React фреймворк с App Router
- **TypeScript** - Типизация для надежности
- **CSS Modules** - Изолированные стили компонентов
- **Font Awesome** - Иконки
- **Google Fonts** - Шрифт Montserrat

## Команды

```bash
# Разработка
npm run dev          # Запустить dev-сервер на http://localhost:3000

# Продакшн
npm run build        # Создать оптимизированную сборку
npm start            # Запустить продакшн-сервер

# Проверка кода
npm run lint         # Проверить код на ошибки
```

## Деплой

### Vercel (Рекомендуется)

1. Создайте аккаунт на [vercel.com](https://vercel.com)
2. Установите Vercel CLI: `npm i -g vercel`
3. Выполните: `vercel`
4. Следуйте инструкциям

### Другие платформы

Проект совместим с:
- Netlify
- Railway
- DigitalOcean App Platform
- AWS Amplify
- И другими платформами с поддержкой Node.js

## Поддержка браузеров

- Chrome (последние 2 версии)
- Firefox (последние 2 версии)
- Safari (последние 2 версии)
- Edge (последние 2 версии)

## Производительность

Проект оптимизирован для:
- ⚡ Быстрой загрузки страниц
- 🎨 Плавных анимаций
- 📱 Мобильных устройств
- ♿ Доступности (a11y)

## Часто задаваемые вопросы

### Как изменить цвета?

Откройте `app/page.module.css` и измените CSS-переменные или найдите цвета (#dc2626 - красный) и замените на нужные.

### Как добавить новую страницу?

1. Создайте папку в `app/`, например `app/services/`
2. Создайте файл `page.tsx` в этой папке
3. Страница автоматически будет доступна по адресу `/services`

### Как изменить тексты?

Все тексты находятся в объекте `translations` в файле `app/page.tsx`

### Как убрать заглушки?

См. раздел "Как заменить заглушки на реальный контент" выше

## Контакты

По вопросам: info@dgt-exhaust.com

## Лицензия

© 2025 DGT Exhaust System. Все права защищены.
